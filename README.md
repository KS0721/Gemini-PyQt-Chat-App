# 💬 [프로젝트명: Gemini AI Desktop Chat Client (DB Persistence)]

[![GitHub license](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)
[![Python](https://img.shields.io/badge/Python-3.9%2B-blue?logo=python)](https://www.python.org/)
[![PyQt5](https://img.shields.io/badge/Framework-PyQt5-41C2C5?logo=qt)](https://www.qt.io/)
[![Google Gemini API](https://img.shields.io/badge/AI_Model-Gemini%202.5%20Flash-00A650?logo=google)](https://ai.google.dev/)

> **PyQt5 GUI 환경에서 Gemini AI와 대화하고, 모든 대화 기록을 SQLite 데이터베이스에 저장 및 검색할 수 있는 데스크톱 클라이언트입니다.**
>
> **핵심 기술:** Python, PyQt5 (GUI), Gemini API, **SQLite3 (DB)**

---

## 💡 프로젝트 개요 및 핵심 기능

본 프로젝트는 AI 연동 기능에 **데이터 관리 기능**을 통합하여, 사용자 친화적인 데스크톱 환경에서 **정보의 영속성(Persistence)**을 구현했습니다. **DB 연동 역량**과 **AI 서비스 통합 능력**을 동시에 보여줍니다.

### 🎯 구현 특징 (기술적 성취)

1.  **데이터 영속성 및 관리 (SQLite3):**
    * Python 내장 **`sqlite3`** 모듈을 활용하여 모든 질문과 답변을 **`chat_history.db`** 파일에 **자동으로 저장**하도록 구현했습니다.
    * **SQL `LIKE` 구문**을 사용하여 질문/답변 내용을 기반으로 과거 기록을 효율적으로 검색하는 기능을 통합했습니다.
2.  **모드 전환 UI 구현:**
    * **QComboBox** 위젯을 사용하여 **'대화'** 모드와 **'검색'** 모드를 선택할 수 있도록 UI 기능을 확장했습니다.
    * 하나의 액션 핸들러(`handle_action`)가 선택된 모드에 따라 동작을 분기 처리하도록 설계하여 **코드의 모듈성**을 높였습니다.
3.  **API 키 보안 강화:**
    * `python-dotenv` 라이브러리를 통해 API 키를 `.env` 파일에서 로드하여 보안을 강화했습니다.
4.  **GUI/로직 분리:**
    * **Qt Designer**(`gemini.ui`)와 **PyQt5**(`gemini.py`)를 분리하여, UI와 백엔드 로직이 독립적으로 관리되는 모듈화된 구조를 적용했습니다.

---

## 🛠️ 기술 스택 및 환경

| 분류 | 기술 | 역할 및 사용 이유 |
| :--- | :--- | :--- |
| **GUI** | **PyQt5** | Python에서 가장 강력하고 유연한 데스크톱 GUI 프레임워크 |
| **데이터베이스** | **SQLite3** | Python 표준 라이브러리를 사용하여 별도의 서버 없이 로컬 데이터 영속성 구현 |
| **환경 변수** | **python-dotenv** | `.env` 파일을 활용한 API 키의 안전한 관리 |
| **AI 모델** | **Gemini 2.5 Flash** | 빠른 응답 속도와 우수한 범용성을 가진 경량 LLM 모델 |
---

## ⚙️ 설치 및 실행 방법

이 프로젝트는 `gemini.py`, `gemini.ui`, 그리고 `.env` 파일이 **모두 같은 디렉터리**에 있어야 실행됩니다.

### 1. 라이브러리 설치

> SQLite3은 Python 표준 라이브러리이므로 별도 설치가 필요 없습니다. 아래 라이브러리만 설치합니다.

```bash
pip install -r requirements.txt

### 2. `.env` 파일 생성 및 키 설정 (필수)

> 프로젝트 루트 디렉터리에 **`.env`** 파일을 생성하고, 발급받은 **Gemini API 키**를 다음과 같이 설정합니다.
>
> **파일명:** `.env`
> `GEMINI_API_KEY="[발급받은_API_키를_여기에_입력]"`

### 3. 애플리케이션 실행

> `gemini.py` 파일이 있는 위치에서 다음 명령어를 실행합니다.
> `python gemini.py`

---
